import React from 'react';
import '../styles/LoadingSpinner.css'; // Separate CSS file for styling

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="futuristic-spinner"></div>
    </div>
  );
};

export default Spinner;
