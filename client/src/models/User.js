class User {
  constructor(id, username, email, firstName, lastName, icon) {
    this.id = id;
    this.username = username;
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.icon = icon;
  }
}
